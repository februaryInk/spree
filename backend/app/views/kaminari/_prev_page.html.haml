%li{:class => "previous #{"disabled" if current_page.first?}"}
  - if current_page.first?
    %a{:href => "javascript:;"} «
  - else
    = link_to_unless current_page.first?, "&laquo;".html_safe, url, rel: 'prev', remote: remote
